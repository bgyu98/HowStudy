<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.dao.ReportDAO">
	<insert id="insertReport" parameterType="ReportVO">
		insert into REPORT (rNum, rClass, mID, rTitle, rOpponent, rDate, rReason) 
		values(nextval(sq_REPORT_SEQ), #{rClass}, #{mId}, #{rTitle}, #{rOpponent}, NOW(), #{rReason})
	</insert>
	
	
	
</mapper>