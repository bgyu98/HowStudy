<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.dao.ReportDAO">
	<insert id="insertReport" parameterType="ReportVO">
		insert into REPORT (rNum, rClass, mID, rTitle, rOpponent, rDate, rReason, rFile) 
		values(nextval(sq_REPORT_SEQ), #{rClass}, #{mId}, #{rTitle}, #{rOpponent}, NOW(), #{rReason}, #{rFile})
	</insert>
	
	<!-- 글 리스트 출력 -->
	 <select id="getReportList" parameterType="ReportVO" resultType="ReportVO">
        SELECT  *
        FROM REPORT
		ORDER BY rNUM DESC
		LIMIT 7
    </select> 
	
	
</mapper>